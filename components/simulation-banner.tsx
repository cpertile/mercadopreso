export function SimulationBanner() {
  return (
    <div className="bg-[#333] px-3 py-1.5 text-center text-[11px] font-medium leading-snug text-white sm:text-xs">
      Simulação — nada é cobrado, nada é enviado, os produtos não existem.
    </div>
  );
}
